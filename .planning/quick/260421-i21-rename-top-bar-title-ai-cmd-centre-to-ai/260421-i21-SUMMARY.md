---
phase: quick-260421-i21
plan: 01
subsystem: ui-layout
tags: [ui-copy, rename, topbar]
requires: []
provides:
  - "Top bar header rendering 'AI_CONTROL_CENTRE'"
affects:
  - src/components/layout/TopBar.tsx
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - src/components/layout/TopBar.tsx
decisions:
  - "Preserved identifier-style underscore form (AI_CONTROL_CENTRE) to match Command Horizon all-caps architectural-label aesthetic — no casing, spacing, or typographic change"
metrics:
  duration: 41s
  completed_at: "2026-04-21T05:02:57Z"
  tasks: 1
  files_changed: 1
requirements:
  - QUICK-260421-i21
---

# Quick Task 260421-i21: Rename Top Bar Title AI_CMD_CENTRE → AI_CONTROL_CENTRE Summary

One-line change in `src/components/layout/TopBar.tsx` line 18 renaming the top bar `<h1>` text from `AI_CMD_CENTRE` to `AI_CONTROL_CENTRE` to align with the corrected product name while preserving the Command Horizon all-caps architectural-label aesthetic.

## Objective

Align the top bar title with the corrected product name — pure UI copy change, no logic, styling, or component-structure modification.

## Tasks Completed

### Task 1: Rename AI_CMD_CENTRE to AI_CONTROL_CENTRE in TopBar.tsx

- **Files modified:** `src/components/layout/TopBar.tsx` (line 18)
- **Change:** `<h1>` inner text `AI_CMD_CENTRE` → `AI_CONTROL_CENTRE`
- **Commit:** `b8963eb` — `chore(ui): rename top bar title AI_CMD_CENTRE → AI_CONTROL_CENTRE`

Exactly one string literal was replaced. Surrounding JSX, className tokens, imports, component structure, and indentation are byte-identical to the pre-change file.

## Verification

All four plan-mandated checks passed:

| # | Check | Result |
|---|-------|--------|
| 1 | `grep -c 'AI_CONTROL_CENTRE' src/components/layout/TopBar.tsx` | `1` |
| 2 | `grep -rn 'AI_CMD_CENTRE' src --exclude-dir=worktrees` | no matches |
| 3 | `git diff --stat src/components/layout/TopBar.tsx` | `1 file changed, 1 insertion(+), 1 deletion(-)` |
| 4 | `git status --porcelain \| grep '^.. \.claude/worktrees/'` | no matches |

Success criteria:

- [x] TopBar `<h1>` renders `AI_CONTROL_CENTRE`
- [x] Zero occurrences of `AI_CMD_CENTRE` under `src/`
- [x] No changes outside `src/components/layout/TopBar.tsx`
- [x] No files under `.claude/worktrees/**` modified
- [x] `git diff --stat` shows exactly 1 file, 1 line added, 1 line removed

## Deviations from Plan

None — plan executed exactly as written. Single task, single line, no Rule 1/2/3 auto-fixes triggered, no Rule 4 checkpoints needed.

## Decisions Made

- Kept the underscore-delimited all-caps identifier form (`AI_CONTROL_CENTRE`) rather than using natural-language casing (e.g. `AI Control Centre`). Rationale: Command Horizon design system specifies architectural, identifier-style labels for navigation/headline surfaces (per CLAUDE.md font + aesthetic notes), and the `<h1>` uses `font-headline tracking-tighter` which was tuned for this casing.

## Commits

- `b8963eb` — `chore(ui): rename top bar title AI_CMD_CENTRE → AI_CONTROL_CENTRE`

## Self-Check: PASSED

- Modified file present at committed state: `src/components/layout/TopBar.tsx` (line 18 now reads `AI_CONTROL_CENTRE`) — FOUND
- Commit `b8963eb` present in `git log` — FOUND
- Grep for old token in `src/` (excluding worktrees) — 0 matches
- Grep for new token in target file — exactly 1 match
