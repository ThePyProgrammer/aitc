---
phase: 13-implement-4-level-semantic-zoom-workspace-package-blobs-only
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/views/Radar/__tests__/semanticZoom.test.ts
  - src/views/Radar/__tests__/packageBlobs.test.ts
  - src/views/Radar/__tests__/CodePreviewOverlay.test.tsx
  - src/views/Radar/__tests__/GraphRenderer.test.ts
autonomous: true
requirements: [VIZN-01, VIZN-04, VIZN-05, DSGN-01, DSGN-04]
must_haves:
  truths:
    - "Semantic zoom anchors 0.6, 2, and 4 are test-locked before implementation."
    - "Package blob aggregation and bridge exclusion are test-locked before implementation."
    - "Code preview fallback, cap, and expand/collapse behavior are test-locked before implementation."
  artifacts:
    - path: "src/views/Radar/__tests__/semanticZoom.test.ts"
      provides: "Semantic level, crossfade, and hit-dominance tests"
      contains: "0.6"
    - path: "src/views/Radar/__tests__/packageBlobs.test.ts"
      provides: "Package blob aggregation and cache tests"
      contains: "kind === 'bridge'"
    - path: "src/views/Radar/__tests__/CodePreviewOverlay.test.tsx"
      provides: "Code preview card component tests"
      contains: "SIGNATURES_UNAVAILABLE"
    - path: "src/views/Radar/__tests__/GraphRenderer.test.ts"
      provides: "Updated file-level renderer tests"
      contains: "zoom 2"
  key_links:
    - from: "src/views/Radar/__tests__/semanticZoom.test.ts"
      to: "src/views/Radar/semanticZoom.ts"
      via: "imports resolveSemanticZoom and constants"
      pattern: "resolveSemanticZoom"
    - from: "src/views/Radar/__tests__/packageBlobs.test.ts"
      to: "src/views/Radar/packageBlobs.ts"
      via: "imports derivePackageBlobs"
      pattern: "derivePackageBlobs"
    - from: "src/views/Radar/__tests__/CodePreviewOverlay.test.tsx"
      to: "src/views/Radar/CodePreviewOverlay.tsx"
      via: "renders component"
      pattern: "CodePreviewOverlay"
---

<objective>
Create the Wave 0 Nyquist test scaffold for Phase 13 before production semantic zoom implementation begins.

Purpose: Lock the user decisions D-01..D-17 and UI-SPEC contracts as executable tests so implementation cannot drift into another visibility-gate patch.
Output: Three new test files and one updated GraphRenderer test file with initially failing assertions or `.todo` entries that name the production exports they expect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-CONTEXT.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-VALIDATION.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-PATTERNS.md
@src/views/Radar/__tests__/GraphRenderer.test.ts
@src/views/Radar/__tests__/hullCache.test.ts
@src/views/Radar/__tests__/RadarCanvas.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold semantic-level resolver tests</name>
  <files>src/views/Radar/__tests__/semanticZoom.test.ts</files>
  <read_first>
    - src/views/Radar/__tests__/GraphRenderer.test.ts
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-CONTEXT.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-VALIDATION.md
  </read_first>
  <behavior>
    - Test anchors: `resolveSemanticZoom(0.59).dominantLevel === 'workspace'`, `resolveSemanticZoom(0.6).dominantLevel === 'package'`, `resolveSemanticZoom(2).dominantLevel === 'file'`, `resolveSemanticZoom(4).dominantLevel === 'code'` per D-01.
    - Test labels: `semanticLabelForLevel('workspace') === 'WORKSPACE'`, package/file/code return `PACKAGE`, `FILE`, `CODE` per D-04.
    - Test crossfade band: anchor `2` crossfades linearly across `[1.9, 2.1]`; outside the band opacities are exactly `1` and `0` per D-02.
    - Test hit dominance: at exact opacity tie `0.5`, `hitLevel` is the higher-detail level per D-03 and UI-SPEC line 157.
  </behavior>
  <action>Create `semanticZoom.test.ts` importing `SEMANTIC_ANCHORS`, `CROSSFADE_HALF_BAND`, `resolveSemanticZoom`, and `semanticLabelForLevel` from `../semanticZoom`. Use concrete expected constants: anchors `[0.6, 2, 4]`, half-band `0.10`, labels `WORKSPACE`, `PACKAGE`, `FILE`, `CODE`. Do not use snapshots. Tests may fail until Plan 13-02 creates `semanticZoom.ts`; that is the intended Wave 0 red state.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/semanticZoom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/views/Radar/__tests__/semanticZoom.test.ts | grep -c "0.6"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/semanticZoom.test.ts | grep -c "CROSSFADE_HALF_BAND"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/semanticZoom.test.ts | grep -c "WORKSPACE"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/semanticZoom.test.ts | grep -c "higher-detail"` returns at least `1`.
  </acceptance_criteria>
  <done>Semantic resolver tests encode D-01, D-02, D-03, D-04, anchor values `0.6`, `2`, `4`, and labels `WORKSPACE`/`PACKAGE`/`FILE`/`CODE`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold package blob aggregation tests</name>
  <files>src/views/Radar/__tests__/packageBlobs.test.ts</files>
  <read_first>
    - src/views/Radar/__tests__/hullCache.test.ts
    - src/views/Radar/hullCache.ts
    - src/stores/radarStore.ts
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-CONTEXT.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-PATTERNS.md
  </read_first>
  <behavior>
    - Test workspace mode returns top-level package blobs only per D-05.
    - Test package mode returns sub-package blobs plus file-dot membership per D-07.
    - Test blob diameter uses square-root scaling: workspace `clamp(24, sqrt(fileCount) * 8, 96)` and package `clamp(20, sqrt(fileCount) * 8, 72)` per UI-SPEC.
    - Test aggregation: `contentionScore` is max child score, `conflictCount` counts child conflict paths, and `activeAgentCount` counts attached current-agent files per D-06/D-15/D-16.
    - Test bridge exclusion: nodes with `kind === 'bridge'` never contribute to fileCount or package centroid.
  </behavior>
  <action>Create `packageBlobs.test.ts` with the existing Path2D jsdom polyfill pattern at the top. Import `derivePackageBlobs`, `selectWorkspaceBlobs`, `selectPackageBlobs`, `blobDiameterPx`, and `_resetPackageBlobCacheForTest` from `../packageBlobs`. Use fixture nodes with ids `src/App.tsx`, `src/views/Radar/RadarCanvas.tsx`, `src/stores/radarStore.ts`, `src-tauri/src/lib.rs`, and `bridge:get_tree_index`. Include literal bridge exclusion assertion text `kind === 'bridge'` in a test name or assertion message for auditability.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/packageBlobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/views/Radar/__tests__/packageBlobs.test.ts | grep -c "derivePackageBlobs"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/packageBlobs.test.ts | grep -c "kind === 'bridge'"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/packageBlobs.test.ts | grep -c "sqrt"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/packageBlobs.test.ts | grep -c "activeAgentCount"` returns at least `1`.
  </acceptance_criteria>
  <done>Package blob tests encode D-05, D-06, D-07, D-08, D-15, D-16, the UI-SPEC diameter formulas, and bridge exclusion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold code preview and renderer regression tests</name>
  <files>src/views/Radar/__tests__/CodePreviewOverlay.test.tsx, src/views/Radar/__tests__/GraphRenderer.test.ts</files>
  <read_first>
    - src/views/Radar/__tests__/RadarCanvas.test.tsx
    - src/views/Radar/__tests__/GraphRenderer.test.ts
    - src/views/Radar/GraphRenderer.ts
    - src/views/Radar/AgentTooltip.tsx
    - src/hooks/useSyntaxHighlight.ts
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-VALIDATION.md
  </read_first>
  <behavior>
    - Code preview tests assert max `6` rendered cards per T-13-04 and UI-SPEC line 189.
    - Fallback copy asserts `PATH_METADATA`, `SIGNATURES_UNAVAILABLE`, `No exported symbols detected.`, `EXPAND_SNIPPET`, and `COLLAPSE_SNIPPET`.
    - Expand/collapse tests assert local state changes without any repository edit/write action.
    - Bounds tests assert card left/top styles stay inside an `8px` inset with width `320px` and max height `240px`.
    - GraphRenderer tests assert file labels render at zoom `2` and non-IPC edge gating preserves `invokes`/`handles` edge visibility at low semantic levels.
  </behavior>
  <action>Create `CodePreviewOverlay.test.tsx` using Testing Library and jsdom shims from `RadarCanvas.test.tsx`. Extend `GraphRenderer.test.ts` by replacing obsolete assumptions that file labels require zoom `4`; assert `drawFileLabels` calls `fillText` at zoom `2`. Add edge-filter tests for a new exported helper `filterEdgesForSemanticLevel(edges, level)` where `workspace` and `package` keep only `kind === 'invokes'` or `kind === 'handles'`, while `file` and `code` keep imports too. Do not add subjective visual assertions.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/CodePreviewOverlay.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/views/Radar/__tests__/CodePreviewOverlay.test.tsx | grep -c "SIGNATURES_UNAVAILABLE"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/CodePreviewOverlay.test.tsx | grep -c "PATH_METADATA"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/CodePreviewOverlay.test.tsx | grep -c "EXPAND_SNIPPET"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/GraphRenderer.test.ts | grep -c "filterEdgesForSemanticLevel"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/__tests__/GraphRenderer.test.ts | grep -c "zoom 2"` returns at least `1`.
  </acceptance_criteria>
  <done>Code preview tests encode D-09, D-10, D-11, D-12, T-13-02, T-13-04; GraphRenderer tests encode file labels at `>= 2` and IPC edge preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture -> production API | Tests name production exports before implementation exists. |
| Code text fixture -> React DOM | Code preview tests must verify JSX text rendering and no unsanitized HTML injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering / Information Disclosure | Code preview backend command tests | transfer | No backend command is created in this Wave 0 test plan; Plan 13-04 owns repo-root canonicalization tests. |
| T-13-02 | Tampering | CodePreviewOverlay tests | mitigate | Assert fallback/signature strings render as text and no test introduces `dangerouslySetInnerHTML` outside existing highlighter contract. |
| T-13-03 | Denial of Service | Signature extraction tests | transfer | No parser code is created in this plan; Plan 13-04 owns file-size and parse-time guards if backend extraction is added. |
| T-13-04 | Denial of Service | packageBlobs and CodePreviewOverlay tests | mitigate | Assert signature cards are capped at `6`, package membership excludes bridges, and derivation uses cached helper exports. |
</threat_model>

<verification>
Run targeted Wave 0 commands:
- `npm run test -- src/views/Radar/__tests__/semanticZoom.test.ts`
- `npm run test -- src/views/Radar/__tests__/packageBlobs.test.ts`
- `npm run test -- src/views/Radar/__tests__/CodePreviewOverlay.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts`

Expected immediately after this plan: tests may fail because production exports do not exist yet. That red state is acceptable only for Wave 0. Plans 13-02 through 13-05 must turn these tests green.
</verification>

<success_criteria>
- Wave 0 files exist with test cases referencing anchors `0.6`, `2`, `4`.
- Every D-01..D-17 decision has at least one explicit test assertion or downstream test hook.
- No production source file is modified in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-01-SUMMARY.md`
</output>
