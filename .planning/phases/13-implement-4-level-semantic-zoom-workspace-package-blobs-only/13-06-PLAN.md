---
phase: 13-implement-4-level-semantic-zoom-workspace-package-blobs-only
plan: 06
type: execute
wave: 4
depends_on: [13-05]
files_modified:
  - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md
autonomous: false
requirements: [VIZN-01, VIZN-04, VIZN-05, DSGN-01, DSGN-04]
must_haves:
  truths:
    - "D-01: UAT verifies zoom anchors 0.6, 2, and 4 define workspace/package/file/code transitions."
    - "D-02: A human can verify semantic zoom feels like a morph rather than a hard snap."
    - "D-03: UAT verifies only the dominant representation handles hover/click during crossfade."
    - "D-04: UAT verifies the HUD label changes exactly WORKSPACE, PACKAGE, FILE, CODE."
    - "D-05: UAT verifies workspace zoom shows top-level package blobs only."
    - "D-06: UAT verifies package blobs encode size plus activity/heat."
    - "D-07: UAT verifies package zoom shows sub-package blobs plus unlabeled file dots."
    - "D-08: UAT verifies blob labels are importance-filtered."
    - "D-09: UAT verifies code zoom starts with signatures/exported symbols."
    - "D-10: UAT verifies code previews are capped to a focused subset, not every visible file."
    - "D-11: UAT verifies signature fallback uses path metadata when scan data is unavailable."
    - "D-12: UAT verifies EXPAND_SNIPPET reveals capped read-only raw snippets."
    - "D-13: UAT verifies bridge nodes stay visible at every semantic zoom level."
    - "D-14: UAT verifies agent dots attach to package centroids at collapsed levels and exact file positions at file/code levels."
    - "D-15: UAT verifies heat/conflict signals aggregate upward into blobs."
    - "D-16: UAT verifies conflicts override heat/activity and agents remain visible."
    - "D-17: UAT verifies wheel zoom, pan, and minimap behavior remain unchanged."
    - "A human can verify package-click focus behavior without graph layout mutation."
    - "Automated Phase 13 targeted suite and build are green before manual sign-off."
  artifacts:
    - path: ".planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md"
      provides: "Manual semantic zoom UAT checkpoint"
      contains: "WORKSPACE"
  key_links:
    - from: "13-06-CHECKPOINT.md"
      to: "13-UI-SPEC.md"
      via: "manual verification steps reference approved UI contract"
      pattern: "0.6"
---

<objective>
Run final automated validation and create the blocking human verification checkpoint for Phase 13 semantic zoom.

Purpose: Confirm the implementation satisfies the observable UI contract that cannot be fully proven by unit tests: smooth semantic morphing and package-click focus behavior.
Output: Green targeted validation evidence and a checkpoint file with exact manual smoke instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-CONTEXT.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-VALIDATION.md
@src/views/Radar/RadarCanvas.tsx
@src/views/Radar/CodePreviewOverlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated semantic zoom gate</name>
  <files>src/views/Radar/RadarCanvas.tsx, src/views/Radar/CodePreviewOverlay.tsx, src/views/Radar/semanticZoom.ts, src/views/Radar/packageBlobs.ts, src/views/Radar/PackageBlobRenderer.ts, src/views/Radar/GraphRenderer.ts, src/stores/radarStore.ts</files>
  <read_first>
    - src/views/Radar/RadarCanvas.tsx
    - src/views/Radar/CodePreviewOverlay.tsx
    - src/views/Radar/semanticZoom.ts
    - src/views/Radar/packageBlobs.ts
    - src/views/Radar/PackageBlobRenderer.ts
    - src/views/Radar/GraphRenderer.ts
    - src/stores/radarStore.ts
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-VALIDATION.md
  </read_first>
  <action>Run final Phase 13 targeted frontend tests, Rust signature tests if Plan 13-04 added backend extraction, and builds. Capture exact command output summaries in the plan summary. Do not fix unrelated pre-existing failures outside Phase 13 files; if present, document the failing test names and two-layer evidence per user memory. Required commands: `npm run test -- src/views/Radar/__tests__/semanticZoom.test.ts src/views/Radar/__tests__/packageBlobs.test.ts src/views/Radar/__tests__/CodePreviewOverlay.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts src/views/Radar/__tests__/RadarCanvas.test.tsx`, `npm run build`, and if `get_source_signatures` exists, `cargo test --manifest-path src-tauri/Cargo.toml --lib pipeline::deps pipeline::commands` or the closest valid scoped cargo commands.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/semanticZoom.test.ts src/views/Radar/__tests__/packageBlobs.test.ts src/views/Radar/__tests__/CodePreviewOverlay.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts src/views/Radar/__tests__/RadarCanvas.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Phase 13 Vitest command exits `0`.
    - `npm run build` exits `0`.
    - `grep -R "v1\|placeholder\|hardcoded for now\|future enhancement" src/views/Radar/semanticZoom.ts src/views/Radar/packageBlobs.ts src/views/Radar/PackageBlobRenderer.ts src/views/Radar/CodePreviewOverlay.tsx src/views/Radar/RadarCanvas.tsx` returns no scope-reduction matches.
  </acceptance_criteria>
  <done>All automated Phase 13 gates are green or unrelated pre-existing failures are documented without out-of-scope edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human semantic zoom UAT checkpoint</name>
  <files>.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md</files>
  <read_first>
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-VALIDATION.md
    - src/views/Radar/RadarCanvas.tsx
    - src/views/Radar/CodePreviewOverlay.tsx
  </read_first>
  <action>Create `13-06-CHECKPOINT.md` with exact manual verification steps: (1) run `npm run tauri dev`; (2) open Radar; (3) wheel through zoom anchors `0.6`, `2`, and `4`; (4) verify HUD label changes exactly `WORKSPACE` → `PACKAGE` → `FILE` → `CODE`; (5) verify workspace shows top-level package blobs only while bridges/agents/conflicts remain visible; (6) verify package shows sub-package blobs plus unlabeled file dots; (7) verify file shows file names and non-IPC edges; (8) verify code shows at most `6` cards with signatures or `PATH_METADATA` / `SIGNATURES_UNAVAILABLE`; (9) click `EXPAND_SNIPPET` then `COLLAPSE_SNIPPET`; (10) click a package blob and verify viewport focuses toward it without pinning/mutating graph layout; (11) press Escape and verify expanded snippets/cards close before bridge deselection. Include resume signals: type `approved` or describe issues. Do not ask the human to perform setup Claude can automate beyond running/observing the app.</action>
  <verify>
    <automated>test -f .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md && grep -v '^#' .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md | grep -c "WORKSPACE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md | grep -c "0.6"` returns at least `1`.
    - `grep -v '^#' .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md | grep -c "EXPAND_SNIPPET"` returns at least `1`.
    - `grep -v '^#' .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-CHECKPOINT.md | grep -c "approved"` returns at least `1`.
  </acceptance_criteria>
  <what-built>Four-level semantic zoom in the Radar: workspace/package/file/code representations, semantic HUD label, package focus, and CODE signature cards.</what-built>
  <how-to-verify>
    Follow the numbered checklist in `13-06-CHECKPOINT.md` against the running Tauri app.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the exact visual/interaction issue.</resume-signal>
  <done>Human has a blocking checkpoint with concrete expected outcomes for smooth morphing and package/code interaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human input -> UAT approval | Human approval controls phase closure. |
| Runtime app -> visual inspection | Manual smoke observes rendered source-derived labels/cards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering / Information Disclosure | Final validation | mitigate | Verify any backend source read command is repo-scoped through Plan 13-04 evidence before approval. |
| T-13-02 | Tampering | Manual CODE card inspection | mitigate | Check no raw HTML/script renders in code cards; source text appears as text or escaped highlighter output. |
| T-13-03 | Denial of Service | Final parser tests | mitigate | Run scoped backend tests if signature extraction exists to confirm file-size/parse-time guard coverage. |
| T-13-04 | Denial of Service | Manual + automated card/label checks | mitigate | Verify max `6` cards, label culling, and no package derivation in rAF via automated evidence. |
</threat_model>

<verification>
- `npm run test -- src/views/Radar/__tests__/semanticZoom.test.ts src/views/Radar/__tests__/packageBlobs.test.ts src/views/Radar/__tests__/CodePreviewOverlay.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts src/views/Radar/__tests__/RadarCanvas.test.tsx`
- `npm run build`
- Manual checkpoint in `13-06-CHECKPOINT.md`
</verification>

<success_criteria>
- Automated Phase 13 targeted suite and build pass.
- Checkpoint file exists with concrete steps and resume signal.
- Phase remains within scope: no OffscreenCanvas/WebGL rewrite, no minimap/pan/zoom retune, no destructive actions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-06-SUMMARY.md`
</output>
