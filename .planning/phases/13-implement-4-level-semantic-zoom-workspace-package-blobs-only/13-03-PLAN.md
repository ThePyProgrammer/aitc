---
phase: 13-implement-4-level-semantic-zoom-workspace-package-blobs-only
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - src/views/Radar/RadarCanvas.tsx
  - src/views/Radar/__tests__/RadarCanvas.test.tsx
autonomous: true
requirements: [VIZN-01, VIZN-04, VIZN-05, DSGN-01, DSGN-04]
must_haves:
  truths:
    - "Workspace zoom renders package blobs only while bridges, agents, and conflicts remain visible."
    - "Package zoom renders sub-package blobs plus unlabeled file dots."
    - "File zoom renders file dots, file names, non-IPC edges, IPC edges, agents, and conflicts."
    - "Zoom HUD shows numeric zoom plus WORKSPACE/PACKAGE/FILE/CODE semantic label."
    - "Agent dots attach to package blob centroids at collapsed levels and crossfade/snap to exact file positions around the package-to-file transition."
  artifacts:
    - path: "src/views/Radar/RadarCanvas.tsx"
      provides: "Semantic representation orchestration, semantic agent positioning, HUD label, hit-test routing, package-click focus"
      contains: "resolveSemanticZoom"
    - path: "src/views/Radar/__tests__/RadarCanvas.test.tsx"
      provides: "Radar integration tests for semantic layers"
      contains: "WORKSPACE"
  key_links:
    - from: "src/views/Radar/RadarCanvas.tsx"
      to: "src/views/Radar/semanticZoom.ts"
      via: "resolveSemanticZoom(viewport.zoom)"
      pattern: "resolveSemanticZoom"
    - from: "src/views/Radar/RadarCanvas.tsx"
      to: "src/views/Radar/packageBlobs.ts"
      via: "derivePackageBlobs outside rAF dependencies"
      pattern: "derivePackageBlobs"
    - from: "src/views/Radar/RadarCanvas.tsx"
      to: "src/views/Radar/PackageBlobRenderer.ts"
      via: "drawPackageBlobs and findPackageBlobAtWorld"
      pattern: "drawPackageBlobs"
---

<objective>
Wire semantic zoom into the Radar render loop for workspace, package, and file representations.

Purpose: Make the user-visible Radar change representation across semantic levels without changing wheel zoom, pan behavior, minimap behavior, bridge visibility, or worker physics.
Output: RadarCanvas orchestration that draws package blobs, filters file details by semantic opacity, updates hit-testing dominance, and shows the semantic HUD label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-CONTEXT.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-RESEARCH.md
@.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-PATTERNS.md
@src/views/Radar/RadarCanvas.tsx
@src/views/Radar/semanticZoom.ts
@src/views/Radar/packageBlobs.ts
@src/views/Radar/PackageBlobRenderer.ts
@src/views/Radar/GraphRenderer.ts
@src/views/Radar/BridgeRenderer.ts
@src/views/Radar/CometTrail.ts
@src/views/Radar/__tests__/RadarCanvas.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive semantic state and package models outside paint work</name>
  <files>src/views/Radar/RadarCanvas.tsx, src/views/Radar/__tests__/RadarCanvas.test.tsx</files>
  <read_first>
    - src/views/Radar/RadarCanvas.tsx
    - src/views/Radar/__tests__/RadarCanvas.test.tsx
    - src/views/Radar/semanticZoom.ts
    - src/views/Radar/packageBlobs.ts
    - src/stores/radarStore.ts
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
  </read_first>
  <action>In `RadarCanvas.tsx`, import `resolveSemanticZoom` and `semanticLabelForLevel` from `./semanticZoom`, and `derivePackageBlobs`, `selectWorkspaceBlobs`, `selectPackageBlobs` from `./packageBlobs`. Add a `semantic = resolveSemanticZoom(viewport.zoom)` calculation in React render scope and mirror it into `stateRef`. Derive package blobs in `useMemo` from `graphNodes`, `contentionScores`, `activeConflictPaths`, and `lastAgentFileRef.current`/`agentFileVersion`; do not derive package membership inside the rAF render function. Include active-agent current files by converting `lastAgentFileRef.current` to file paths when `agentFileVersion` changes. Add tests/mocks that prove `derivePackageBlobs` is not called from the rAF body by asserting import usage in component render scope or by verifying it is called only once across two dirty frames with unchanged deps.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/packageBlobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "resolveSemanticZoom"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "derivePackageBlobs"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "useMemo"` returns at least `1` on a line or block deriving package blobs.
    - `npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/packageBlobs.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Semantic state and package blob models are available to the render loop without per-frame hierarchy recomputation, satisfying D-06/D-08/D-15/T-13-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render semantic representations with correct z-order and opacity</name>
  <files>src/views/Radar/RadarCanvas.tsx, src/views/Radar/__tests__/RadarCanvas.test.tsx</files>
  <read_first>
    - src/views/Radar/RadarCanvas.tsx
    - src/views/Radar/PackageBlobRenderer.ts
    - src/views/Radar/GraphRenderer.ts
    - src/views/Radar/BridgeRenderer.ts
    - src/views/Radar/CometTrail.ts
    - src/views/Radar/__tests__/RadarCanvas.test.tsx
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
  </read_first>
  <action>Modify the rAF render sequence in `RadarCanvas.tsx` so rendering and hit-testing are separated per D-02/D-03: use `semantic.hitLevel` and the dominant representation only for hover/click routing, never as the sole draw gate for adjacent crossfade layers. Draw workspace blobs with `semantic.opacityByLevel.workspace`; draw package blobs and unlabeled file dots with `semantic.opacityByLevel.package`. File/code edge rendering must run whenever `semantic.opacityByLevel.file > 0` or `semantic.opacityByLevel.code > 0`; compute `const fileCodeOpacity = Math.min(1, semantic.opacityByLevel.file + semantic.opacityByLevel.code)`, then wrap the file/code draw pass in `ctx.save()`/`ctx.globalAlpha = fileCodeOpacity`/`ctx.restore()`. Inside that file/code draw pass, call `filterEdgesForSemanticLevel(s.graphEdges, 'file')` for rendering before `drawEdges` and `drawArrowHeads`; do not use `filterEdgesForSemanticLevel(s.graphEdges, semantic.dominantLevel)` for rendering because it suppresses required FILE edges during the package-to-file crossfade. Workspace/package-only edge rendering may separately filter to IPC/bridge edges for the bridge spine, but it must not suppress or replace the adjacent file/code edge pass while file/code opacity is greater than zero. Keep bridge boundary, bridge diamonds/labels, semantic agent trails/dots, conflict pulses/badges, and boundary anchor labels after semantic representations per UI-SPEC render order. Derive semantic agent positions per D-14 from active/current agent file paths (`lastAgentFileRef.current` or equivalent), package blob membership (`memberFileIds`), blob centroid, exact file-node live position, and semantic opacities: at workspace/package dominance attach each agent to its package centroid; fan out multiple agents on one blob on an 8px screen-space ring; across the package→file band around zoom `2`, crossfade/interpolate from package centroid to exact file position; at FILE/CODE dominance snap to exact file-node positions. Add RadarCanvas tests asserting centroid attachment, 8px fan-out for multiple agents on one blob, package-to-file crossfade around zoom `2`, and that file/code edges are drawn during the package-to-file crossfade even while package remains dominant for hit-testing. Use `ctx.save()`/`ctx.globalAlpha`/`ctx.restore()` around each representation; reset `globalAlpha` to `1` before bridge/agent/conflict layers.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "drawPackageBlobs"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "filterEdgesForSemanticLevel"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -F -c "filterEdgesForSemanticLevel(s.graphEdges, 'file')"` returns at least `1` and proves file/code edge rendering filters as FILE rather than `semantic.dominantLevel`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -E -c "semantic\.opacityByLevel\.file\s*>\s*0|semantic\.opacityByLevel\.code\s*>\s*0"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "globalAlpha"` returns at least `3`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "drawBridgeNodes"` returns at least `1` and remains after semantic file/package drawing in the render sequence.
    - `npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/GraphRenderer.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Workspace/package/file representations render with crossfade opacity, file/code edges crossfade instead of hard-snapping, IPC/bridge-only package edge filtering never suppresses the adjacent file/code edge pass, agent dots attach/crossfade per D-14, and bridge/agent/conflict overlays preserve their priority per D-13..D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route semantic hit-testing and HUD label</name>
  <files>src/views/Radar/RadarCanvas.tsx, src/views/Radar/__tests__/RadarCanvas.test.tsx</files>
  <read_first>
    - src/views/Radar/RadarCanvas.tsx
    - src/views/Radar/semanticZoom.ts
    - src/views/Radar/PackageBlobRenderer.ts
    - src/hooks/useCanvasZoomPan.ts
    - src/views/Radar/__tests__/RadarCanvas.test.tsx
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-CONTEXT.md
    - .planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-UI-SPEC.md
  </read_first>
  <action>Update `handleMouseMove` and `handleClick` in `RadarCanvas.tsx`: preserve bridge hit-test first; then branch on `resolveSemanticZoom(viewport.zoom).hitLevel`. `semantic.hitLevel` / dominant representation is for hit-testing only and must not be reused to hide adjacent file/code drawing during crossfade. For `workspace`/`package`, call `findPackageBlobAtWorld` on workspace or package blobs and set package hover state without setting `hoveredNodeId`; package click should call `setViewport({ zoom: Math.max(viewport.zoom, 2), panX: canvasWidth / 2 - blob.centroid.x * nextZoom, panY: canvasHeight / 2 - blob.centroid.y * nextZoom })` and must not call `pinNode`. For `file`/`code`, keep existing quadtree file hover behavior. Extend the bottom-left zoom indicator so it renders `{viewport.zoom.toFixed(1)}x` plus a sibling semantic label exactly `WORKSPACE`, `PACKAGE`, `FILE`, or `CODE` using `font-headline`, `text-[10px]`, `font-semibold`, `tracking-[0.12em]`, and `text-primary`. Do not change `useCanvasZoomPan`, minimap, viewport clamp, or fit-to-graph behavior per D-17.</action>
  <verify>
    <automated>npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/semanticZoom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "findPackageBlobAtWorld"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "semanticLabelForLevel"` returns at least `1`.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "WORKSPACE"` returns at least `1` through tests or label mapping usage.
    - `grep -v '^#' src/views/Radar/RadarCanvas.tsx | grep -c "pinNode"` returns `0`.
    - `npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/semanticZoom.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Hit-testing follows bridge first, code card deferred to Plan 13-05, agents, then dominant semantic representation; HUD exposes semantic level; pan/zoom/minimap mechanics are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer input -> semantic hit-test | Untrusted screen coordinates select bridge, package, or file targets. |
| Store graph data -> Canvas/DOM HUD | File paths, package labels, and conflict state affect rendered UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering / Information Disclosure | RadarCanvas semantic rendering | accept | This plan adds no backend read command and no source-content access. |
| T-13-02 | Tampering | Zoom HUD / package labels | mitigate | HUD is static label text from `semanticLabelForLevel`; package labels are Canvas `fillText`, not HTML. |
| T-13-03 | Denial of Service | Parser extraction | accept | This plan adds no parser code. |
| T-13-04 | Denial of Service | RadarCanvas render loop | mitigate | Package derivation is in `useMemo`, label/detail rendering remains viewport/importance gated, and package hierarchy is not recomputed in rAF. |
</threat_model>

<verification>
- `npm run test -- src/views/Radar/__tests__/RadarCanvas.test.tsx src/views/Radar/__tests__/semanticZoom.test.ts src/views/Radar/__tests__/packageBlobs.test.ts src/views/Radar/__tests__/GraphRenderer.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Workspace/package/file levels render according to UI-SPEC lines 145-184.
- Crossfade drawing uses semantic opacity, while hit-testing uses `semantic.hitLevel` / dominant representation only for interaction routing.
- File/code edge rendering runs whenever `semantic.opacityByLevel.file > 0` or `semantic.opacityByLevel.code > 0`, uses `filterEdgesForSemanticLevel(s.graphEdges, 'file')`, and applies file/code opacity via `ctx.globalAlpha`.
- Bridge, agent, conflict, wheel, pan, minimap, and worker behavior remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-implement-4-level-semantic-zoom-workspace-package-blobs-only/13-03-SUMMARY.md`
</output>
