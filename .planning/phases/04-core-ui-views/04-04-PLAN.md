---
phase: 04-core-ui-views
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/views/CommsHub/ChatThread.tsx
  - src/views/CommsHub/ChatInput.tsx
  - src/views/CommsHub/TelemetryPanel.tsx
  - src/views/CommsHub/SystemLoad.tsx
  - src/views/CommsHub/TelemetryFeed.tsx
  - src/views/CommsHub/MiniChatCard.tsx
  - src/views/CommsView.tsx
  - src/components/ui/DeliveryStatus.tsx
  - src-tauri/src/agents/notifications.rs
  - src/views/CommsHub/__tests__/CommsComponents.test.tsx
autonomous: true
requirements: [COMM-04, COMM-05]
must_haves:
  truths:
    - "User can send freeform text messages to an agent in a chat thread"
    - "Chat messages show delivery status indicators (delivered/queued/unsupported)"
    - "User can see system load metrics (CPU, memory) in the right telemetry panel"
    - "User can see mini chat cards for each active agent in the right panel"
    - "Expanding a mini chat card shows last 5 messages"
    - "Native OS notification fires when a new approval request arrives"
    - "System tray icon updates based on pending approvals"
  artifacts:
    - path: "src/views/CommsHub/ChatThread.tsx"
      provides: "Scrollable message timeline"
      contains: "DeliveryStatus"
    - path: "src/views/CommsHub/TelemetryPanel.tsx"
      provides: "Right panel with system load + telemetry + mini chats"
      contains: "SystemLoad"
    - path: "src/components/ui/DeliveryStatus.tsx"
      provides: "Delivery status indicator component"
      contains: "delivered"
    - path: "src/views/CommsHub/__tests__/CommsComponents.test.tsx"
      provides: "Tests for MiniChatCard, ChatThread, TelemetryPanel, DeliveryStatus"
      contains: "describe"
  key_links:
    - from: "src/views/CommsHub/ChatThread.tsx"
      to: "src/stores/commsStore.ts"
      via: "useCommsStore for messages[agentId]"
      pattern: "useCommsStore"
    - from: "src/views/CommsHub/ChatInput.tsx"
      to: "src/stores/commsStore.ts"
      via: "sendMessage action"
      pattern: "sendMessage"
---

<objective>
Complete the Communications Hub: chat thread with message delivery status, right-side telemetry panel with system load metrics and mini chat cards, native OS notification integration for approval requests, and tests for key component behaviors.

Purpose: This delivers the chat interface (COMM-04) and notification system (COMM-05) -- enabling bidirectional agent communication and urgent event alerts.
Output: Fully wired Communications Hub with chat, telemetry panel, OS notifications, and component tests.
</objective>

<execution_context>
@C:/Users/prann/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-ui-views/04-CONTEXT.md
@.planning/phases/04-core-ui-views/04-RESEARCH.md
@.planning/phases/04-core-ui-views/04-UI-SPEC.md

@.planning/phases/04-core-ui-views/04-01-SUMMARY.md
@.planning/phases/04-core-ui-views/04-02-SUMMARY.md

<interfaces>
From src/stores/commsStore.ts (Plan 02):
```typescript
export interface ChatMessage {
  id: number;
  agentId: string;
  direction: 'inbound' | 'outbound';
  content: string;
  deliveryStatus: 'delivered' | 'queued' | 'unsupported';
  approvalRequestId: number | null;
  createdAt: string;
}

// Store actions:
sendMessage: (agentId: string, content: string) => Promise<void>;
fetchMessages: (agentId: string) => Promise<void>;
messages: Record<string, ChatMessage[]>;
```

From src/stores/agentStore.ts:
```typescript
export interface AgentInfo {
  id: string;
  agentType: string;
  state: 'running' | 'idle' | 'waiting' | 'conflict' | 'error';
}
```

From src/stores/pipelineStore.ts:
```typescript
events: FileEvent[];
eventCount: number;
isWatching: boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatThread, ChatInput, DeliveryStatus components</name>
  <files>
    src/views/CommsHub/ChatThread.tsx,
    src/views/CommsHub/ChatInput.tsx,
    src/components/ui/DeliveryStatus.tsx
  </files>
  <read_first>
    src/stores/commsStore.ts,
    src/views/CommsHub/RequestDetail.tsx,
    src/components/ui/StatusBadge.tsx,
    .planning/phases/04-core-ui-views/04-UI-SPEC.md
  </read_first>
  <action>
**Create `src/components/ui/DeliveryStatus.tsx`** per UI-SPEC:
- Three variants:
  - `delivered`: Lucide `Check` icon (16px), primary color (#8eff71), label "DELIVERED"
  - `queued`: Lucide `Clock` icon (16px), tertiary color (#ffd16f), label "QUEUED"
  - `unsupported`: Lucide `X` icon (16px), error color (#ff7351), label "UNSUPPORTED"
- JetBrains Mono 10px (Data-sm role), inline-flex gap-1 layout
- Props: `status: 'delivered' | 'queued' | 'unsupported'`

**Create `src/views/CommsHub/ChatThread.tsx`** per D-13, D-15, UI-SPEC:
- Scrollable message timeline below the diff in RequestDetail
- Props: `agentId: string`
- On mount: call `commsStore.fetchMessages(agentId)` to load history
- Read `commsStore.messages[agentId]` for message list
- Render each message:
  - User messages (direction='outbound'): right-aligned, surface-container bg
  - Agent messages (direction='inbound'): left-aligned, surface-container-low bg
  - Content: JetBrains Mono 14px (Body role)
  - Timestamp: JetBrains Mono 10px (Data-sm), on-surface-variant
  - DeliveryStatus indicator after each message
- Auto-scroll to bottom on new message via `scrollIntoView` on last message ref
- Sorted by `createdAt` timestamp
- Empty state: "NO_MESSAGES" heading, "Send a message to begin communication with this agent." body
- Ghost border (outline-variant 10%) on thread container

**Create `src/views/CommsHub/ChatInput.tsx`** per UI-SPEC ChatInput spec:
- Terminal-style input at bottom of detail panel
- surface-container-lowest fill (#000000), ghost border (outline-variant at 10%)
- Block cursor blink animation in secondary color (#00cffc): use CSS `animation: blink-cursor 1s step-end infinite` with `caret-color: #00cffc`
- Placeholder text: "TYPE_COMMAND_OR_QUERY..." in on-surface-variant
- Send button: Lucide `Send` icon (16px), with `aria-label="Send message"`
- On Enter key or Send click: call `commsStore.sendMessage(agentId, content)`, clear input
- Minimum height 40px, auto-grows up to 120px max (use textarea with auto-resize)
- Props: `agentId: string`

**Update `src/views/CommsHub/RequestDetail.tsx`:**
- Add ChatThread component below the ApprovalActions, passing `agentId` from selected request
- Add ChatInput component at the bottom of the detail panel
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/aitc && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/DeliveryStatus.tsx contains "delivered" and "queued" and "unsupported"
    - src/components/ui/DeliveryStatus.tsx imports from "lucide-react" (Check, Clock, X)
    - src/views/CommsHub/ChatThread.tsx contains "fetchMessages"
    - src/views/CommsHub/ChatThread.tsx contains "DeliveryStatus"
    - src/views/CommsHub/ChatThread.tsx contains "NO_MESSAGES"
    - src/views/CommsHub/ChatInput.tsx contains "TYPE_COMMAND_OR_QUERY"
    - src/views/CommsHub/ChatInput.tsx contains 'aria-label="Send message"' or "aria-label='Send message'"
    - src/views/CommsHub/ChatInput.tsx contains "sendMessage"
    - All tests pass
  </acceptance_criteria>
  <done>Chat thread renders message timeline with delivery status indicators. Chat input sends messages with Enter/Send button. Auto-scrolls to latest message. Empty state shows NO_MESSAGES.</done>
</task>

<task type="auto">
  <name>Task 2: TelemetryPanel, SystemLoad, MiniChatCard + OS notifications</name>
  <files>
    src/views/CommsHub/TelemetryPanel.tsx,
    src/views/CommsHub/SystemLoad.tsx,
    src/views/CommsHub/TelemetryFeed.tsx,
    src/views/CommsHub/MiniChatCard.tsx,
    src/views/CommsView.tsx,
    src-tauri/src/agents/notifications.rs
  </files>
  <read_first>
    src/views/CommsView.tsx,
    src/stores/commsStore.ts,
    src/stores/agentStore.ts,
    src/stores/pipelineStore.ts,
    src-tauri/src/agents/notifications.rs,
    .planning/phases/04-core-ui-views/04-UI-SPEC.md
  </read_first>
  <action>
**Create `src/views/CommsHub/SystemLoad.tsx`** per UI-SPEC:
- CPU and memory usage bars
- Bar fill color: primary (#8eff71) for <70%, tertiary (#ffd16f) for 70-90%, error (#ff7351) for >90%
- Labels: "CPU_CLUSTER" and "MEMORY_SNAP" (Space Grotesk 10px uppercase)
- Values: percentage in JetBrains Mono 12px bold
- Data source: use Tauri `invoke('plugin:system-info|...')` or a simple Tauri command that returns `{ cpu_percent: f64, memory_percent: f64 }`. If no system info command exists yet, create a simple polling mechanism that reads from `sysinfo` crate (already a dependency from Phase 2's process_snapshot.rs). For MVP, display static "N/A" values with the bar UI structure and add a TODO comment for live data wiring.

**Create `src/views/CommsHub/TelemetryFeed.tsx`** per UI-SPEC:
- Header: "TELEMETRY_FEED" (Space Grotesk 14px bold uppercase)
- Scrollable recent events list from `pipelineStore.events`
- Each entry: Lucide icon for event type (FileEdit for write, Eye for read, Trash for delete), agent ID, file path (truncated), timestamp
- JetBrains Mono 10px (Data-sm role)
- Max 50 visible items (slice from events array)

**Create `src/views/CommsHub/MiniChatCard.tsx`** per D-13, UI-SPEC:
- One card per active agent (from agentStore.agents)
- Collapsed (default): 120px height, shows agent ID (JetBrains Mono 12px bold), last message preview (1 line, truncated), unread count badge (primary bg)
- Expanded: auto height, shows last 5 messages from `commsStore.messages[agentId]`
- Click to expand/collapse with Motion `layoutId` animation (150ms ease-out)
- Click agent name: call `commsStore.selectRequest(null)` then navigate to full chat in center panel (set a chatAgentId state in commsStore or local state)
- Expand icon: Lucide `ChevronDown`/`ChevronUp`

**Create `src/views/CommsHub/TelemetryPanel.tsx`** per D-02, UI-SPEC:
- Right panel container, 260px width, surface-container bg
- Contains in order: SystemLoad, TelemetryFeed, MiniChatCard list (one per active agent)
- Each section separated by 24px (lg spacing)

**Update `src/views/CommsView.tsx`:**
- Replace the right panel placeholder with TelemetryPanel component
- Ensure the 3-panel layout is: RequestQueue (280px) | RequestDetail (flex-1) | TelemetryPanel (260px)

**Extend OS notifications in `src-tauri/src/agents/notifications.rs`** per D-16, COMM-05:
- Add a new function `dispatch_approval_notification(app_handle: &AppHandle, agent_id: &str, file_path: &str)`:
  - Title: "APPROVAL_REQUIRED"
  - Body: "{agent_id} requests access to {file_path}"
  - Uses existing `tauri_plugin_notification::NotificationExt` pattern
- This function is called from `create_approval_request` in comms/commands.rs (already wired in Plan 01, verify it calls this)
- If not yet called from Plan 01, add the call in `create_approval_request`
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/aitc && npx vitest run --reporter=verbose 2>&1 | tail -10 && cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/views/CommsHub/TelemetryPanel.tsx contains "SystemLoad" and "TelemetryFeed" and "MiniChatCard"
    - src/views/CommsHub/TelemetryPanel.tsx contains "260px" or "w-[260px]"
    - src/views/CommsHub/SystemLoad.tsx contains "CPU_CLUSTER" and "MEMORY_SNAP"
    - src/views/CommsHub/TelemetryFeed.tsx contains "TELEMETRY_FEED"
    - src/views/CommsHub/MiniChatCard.tsx contains "120px" or "h-[120px]" for collapsed height
    - src/views/CommsHub/MiniChatCard.tsx contains Motion or motion import for animation
    - src/views/CommsView.tsx contains "TelemetryPanel" (no longer placeholder)
    - src-tauri/src/agents/notifications.rs contains "dispatch_approval_notification" or "APPROVAL_REQUIRED"
    - All tests pass
    - cargo check exits 0
  </acceptance_criteria>
  <done>Communications Hub right panel shows system load metrics, telemetry feed of recent file events, and expandable mini chat cards per agent. OS notifications fire for new approval requests. Full 3-panel Comms Hub layout complete.</done>
</task>

<task type="auto">
  <name>Task 3: Component tests for ChatThread, MiniChatCard, TelemetryPanel, DeliveryStatus</name>
  <files>
    src/views/CommsHub/__tests__/CommsComponents.test.tsx
  </files>
  <read_first>
    src/views/CommsHub/ChatThread.tsx,
    src/views/CommsHub/MiniChatCard.tsx,
    src/views/CommsHub/TelemetryPanel.tsx,
    src/components/ui/DeliveryStatus.tsx,
    src/stores/__tests__/commsStore.test.ts,
    src/stores/__tests__/agentStore.test.ts
  </read_first>
  <behavior>
    - Test: DeliveryStatus renders "DELIVERED" with Check icon when status="delivered"
    - Test: DeliveryStatus renders "QUEUED" with Clock icon when status="queued"
    - Test: DeliveryStatus renders "UNSUPPORTED" with X icon when status="unsupported"
    - Test: ChatThread renders messages from commsStore for given agentId
    - Test: ChatThread shows "NO_MESSAGES" empty state when no messages exist
    - Test: ChatThread calls fetchMessages on mount with agentId
    - Test: MiniChatCard renders collapsed at 120px height by default
    - Test: MiniChatCard expands on click to show last 5 messages
    - Test: MiniChatCard shows agent ID and last message preview when collapsed
    - Test: TelemetryPanel mounts SystemLoad, TelemetryFeed, and MiniChatCard sub-panels
  </behavior>
  <action>
Create `src/views/CommsHub/__tests__/CommsComponents.test.tsx` with vitest + React Testing Library tests.

Mock the Zustand stores (commsStore, agentStore, pipelineStore) following the existing test patterns in `src/stores/__tests__/`. Use `vi.mock` for store modules, providing controlled state for each test.

**DeliveryStatus tests (3 tests):**
- Render DeliveryStatus with each status variant, assert correct label text and icon presence via `screen.getByText("DELIVERED")` etc.

**ChatThread tests (3 tests):**
- Mock commsStore with messages for agent "agent-1", render ChatThread with agentId="agent-1", assert messages appear in DOM
- Mock commsStore with empty messages, render ChatThread, assert "NO_MESSAGES" text visible
- Mock commsStore.fetchMessages, render ChatThread, assert fetchMessages was called with "agent-1"

**MiniChatCard tests (3 tests):**
- Render MiniChatCard with agent data, assert collapsed state (agent ID visible, height constrained)
- Simulate click on card, assert expanded state (messages become visible)
- Render with last message, assert preview text is visible in collapsed state

**TelemetryPanel test (1 test):**
- Render TelemetryPanel, assert "CPU_CLUSTER" or "SYSTEM_LOAD" heading visible, "TELEMETRY_FEED" heading visible, and at least one MiniChatCard rendered (mock agentStore with 1 agent)
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/aitc && npx vitest run src/views/CommsHub/__tests__/CommsComponents.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/views/CommsHub/__tests__/CommsComponents.test.tsx contains at least 10 test cases
    - Tests cover DeliveryStatus variants, ChatThread rendering and empty state, MiniChatCard expand/collapse, TelemetryPanel composition
    - All tests pass
  </acceptance_criteria>
  <done>Component tests verify DeliveryStatus renders all 3 variants correctly, ChatThread renders messages and empty state, MiniChatCard expands/collapses, TelemetryPanel composes sub-panels. All tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent messages -> Chat renderer | Agent-provided text displayed in chat thread |
| OS notification content | Agent ID and file path shown in native notifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | ChatThread.tsx (agent message XSS) | mitigate | Render all message content as plain text nodes via JSX. React's default escaping prevents XSS. Never use dangerouslySetInnerHTML |
| T-04-11 | Information Disclosure | OS notifications | accept | Notifications show file paths from user's own codebase. No cross-user exposure. Desktop app context |
</threat_model>

<verification>
- `npx vitest run` all tests pass
- `cargo check` passes
- CommsView shows 3 functional panels: queue | detail | telemetry
- Chat messages send and display with delivery status
- Mini chat cards expand/collapse for each agent
- SystemLoad shows CPU/memory bars
- TelemetryFeed shows recent file events
- Component tests cover key interactive behaviors
</verification>

<success_criteria>
User can send freeform text messages to agents via chat input, see message history with delivery status indicators, view system load metrics and telemetry feed in the right panel, and see mini chat cards for all active agents. Native OS notifications fire when approval requests arrive. Component tests verify MiniChatCard expand/collapse, ChatThread message rendering, TelemetryPanel composition, and DeliveryStatus variants.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-ui-views/04-04-SUMMARY.md`
</output>
