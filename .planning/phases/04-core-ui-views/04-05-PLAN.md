---
phase: 04-core-ui-views
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - src/views/RadarView.tsx
  - src/views/Radar/RadarCanvas.tsx
  - src/views/Radar/RadarManifest.tsx
  - src/views/Radar/AgentManifestRow.tsx
  - src/views/Radar/AgentTooltip.tsx
  - src/views/Radar/RadarMinimap.tsx
  - src/views/Radar/AlertDetail.tsx
  - src/views/Radar/__tests__/RadarComponents.test.tsx
autonomous: false
requirements: [VIZN-02]
must_haves:
  truths:
    - "User sees agent trajectory lead lines from dots to recently-touched files on radar"
    - "User can hover an agent dot to see a tooltip with agent ID, status, file count, intent"
    - "User can click an agent dot or manifest row to select and highlight it"
    - "Right-side manifest panel lists all agents with status and can be collapsed"
    - "Mini map shows full treemap overview with viewport indicator"
    - "Clicking agent in manifest centers radar on that agent"
  artifacts:
    - path: "src/views/Radar/RadarManifest.tsx"
      provides: "Right-side collapsible agent manifest panel"
      contains: "AGENT_MANIFEST"
    - path: "src/views/Radar/AgentTooltip.tsx"
      provides: "HTML overlay tooltip for agent hover"
      contains: "glassmorphism"
    - path: "src/views/Radar/RadarMinimap.tsx"
      provides: "Bottom-right minimap overlay"
      contains: "160"
    - path: "src/views/Radar/__tests__/RadarComponents.test.tsx"
      provides: "Tests for RadarManifest, AgentManifestRow, RadarMinimap"
      contains: "describe"
  key_links:
    - from: "src/views/Radar/RadarManifest.tsx"
      to: "src/stores/radarStore.ts"
      via: "selectAgent action"
      pattern: "selectAgent"
    - from: "src/views/Radar/RadarCanvas.tsx"
      to: "src/stores/pipelineStore.ts"
      via: "file events for lead line computation"
      pattern: "pipelineStore"
---

<objective>
Complete the Airspace Radar with agent trajectory lead lines, interactive manifest panel, agent tooltip on hover, minimap overview, component tests, and visual verification of the full Phase 4 UI.

Purpose: This delivers trajectory visualization (VIZN-02) and completes the radar's interactive features -- the agent manifest panel, tooltip overlays, minimap navigation, and lead line aging.
Output: Fully interactive Airspace Radar with lead lines, manifest, tooltips, minimap, agent highlight, component tests, and human-verified Phase 4 UI.
</objective>

<execution_context>
@C:/Users/prann/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-ui-views/04-CONTEXT.md
@.planning/phases/04-core-ui-views/04-RESEARCH.md
@.planning/phases/04-core-ui-views/04-UI-SPEC.md

@.planning/phases/04-core-ui-views/04-01-SUMMARY.md
@.planning/phases/04-core-ui-views/04-03-SUMMARY.md
@.planning/phases/04-core-ui-views/04-04-SUMMARY.md

<interfaces>
From src/stores/radarStore.ts (Plan 03):
```typescript
export interface Viewport { zoom: number; panX: number; panY: number; }
export const AGENT_DOT_PALETTE: string[];
export function getAgentColor(agentId: string): string;
interface RadarStore {
  treeData: TreeIndexEntry[];
  viewport: Viewport;
  selectedAgentId: string | null;
  isManifestOpen: boolean;
  selectAgent: (id: string | null) => void;
  toggleManifest: () => void;
}
```

From src/hooks/useTreemapLayout.ts (Plan 03):
```typescript
export interface TreemapRect {
  x0: number; y0: number; x1: number; y1: number;
  path: string; name: string; depth: number; isFile: boolean; size: number;
}
```

From src/hooks/useCanvasZoomPan.ts (Plan 03):
```typescript
screenToWorld(screenX: number, screenY: number): { x: number; y: number; }
```

From src/stores/agentStore.ts:
```typescript
export interface AgentInfo {
  id: string; agentType: string;
  state: 'running' | 'idle' | 'waiting' | 'conflict' | 'error';
  intent: string | null;
}
```

From src/stores/pipelineStore.ts:
```typescript
events: FileEvent[];  // Each has path, kind, attribution with agentId
```

From src/views/TowerControl/AgentRow.tsx:
<!-- Reusable pattern for agent row with status badge -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lead lines, RadarManifest, AgentTooltip, RadarMinimap</name>
  <files>
    src/views/RadarView.tsx,
    src/views/Radar/RadarCanvas.tsx,
    src/views/Radar/RadarManifest.tsx,
    src/views/Radar/AgentManifestRow.tsx,
    src/views/Radar/AgentTooltip.tsx,
    src/views/Radar/RadarMinimap.tsx,
    src/views/Radar/AlertDetail.tsx
  </files>
  <read_first>
    src/views/RadarView.tsx,
    src/views/Radar/RadarCanvas.tsx,
    src/stores/radarStore.ts,
    src/stores/agentStore.ts,
    src/stores/pipelineStore.ts,
    src/hooks/useTreemapLayout.ts,
    src/hooks/useCanvasZoomPan.ts,
    src/views/TowerControl/AgentRow.tsx,
    src/views/TowerControl/AgentManifest.tsx,
    src/components/ui/StatusBadge.tsx,
    .planning/phases/04-core-ui-views/04-UI-SPEC.md
  </read_first>
  <action>
**Add lead lines to RadarCanvas.tsx** (D-10, VIZN-02):
- After drawing agent dots, draw lead lines to recently-touched files
- Data source: `pipelineStore.events` filtered by agent ID, last N events per agent (N=10)
- For each recent file event: find the TreemapRect matching the file path, draw a 0.5px line from agent dot center to the target rect center
- Line color: agent dot color at 40% opacity
- Gradient from full opacity at agent dot to 10% at target file (use `ctx.createLinearGradient`)
- Timestamp label at midpoint: JetBrains Mono 10px, Data-sm role
- Lead lines only visible at zoom >= 3 (progressive detail per D-11)
- Lead line fade animation: opacity = max(0.3, 1.0 - (ageMs / 30000)) where ageMs = Date.now() - event timestamp. Lines older than 30s fade to 30% opacity (Lead Line Fade from UI-SPEC)

**Add agent highlight to RadarCanvas.tsx** (Agent Highlight animation):
- When `radarStore.selectedAgentId` is set, draw a 40px radius ambient glow circle behind the selected agent dot at 15% opacity in agent color, with 2s pulsing cycle

**Create `src/views/Radar/RadarManifest.tsx`** per D-12, UI-SPEC:
- Right-side collapsible panel, 280px width, surface-container-low bg (#131313)
- Header: "AGENT_MANIFEST" (Space Grotesk 14px bold uppercase) + collapse toggle (Lucide `ChevronRight` when collapsed, `ChevronLeft` when expanded)
- Lists all agents from `agentStore.agents` via AgentManifestRow
- Collapse/expand with Motion slide animation (200ms ease-in-out): animate width from 280px to 0px
- Read `radarStore.isManifestOpen` and `radarStore.toggleManifest`
- Below agent list: AlertDetail section

**Create `src/views/Radar/AgentManifestRow.tsx`** per UI-SPEC:
- Reuses AgentRow visual pattern from TowerControl
- Shows: 8px colored circle swatch (using `getAgentColor(agent.id)`), agent ID (JetBrains Mono 12px bold), StatusBadge for state, file count (from pipeline events filtered by agent)
- Click handler: calls `radarStore.selectAgent(agent.id)` and centers radar viewport on agent position
  - To center on agent: find the agent's treemap rect, compute center coords, set viewport panX/panY to center that point on screen
- Selected state: surface-container-high bg + 2px left border in agent dot color

**Create `src/views/Radar/AgentTooltip.tsx`** per UI-SPEC:
- HTML overlay (not canvas-rendered), positioned via absolute positioning based on mouse coords
- Only visible when hovering an agent dot (use `hoveredAgentId` state from RadarCanvas)
- Glassmorphism: surface-container-highest bg at 60% opacity, backdrop-blur-[20px]
- Content: agent ID (JetBrains Mono 12px bold), StatusBadge, active file count, intent text (JetBrains Mono 14px, max 2 lines with overflow-hidden)
- Position: offset 12px right and 12px below cursor, clamp to viewport bounds

**Create `src/views/Radar/RadarMinimap.tsx`** per UI-SPEC:
- Bottom-right corner overlay, 160x120px
- surface-container bg at 80% opacity
- Renders a tiny version of the full treemap at minimum zoom (use a second small canvas or div representation)
- White rectangle indicator showing current viewport bounds relative to full treemap
- Click to jump viewport: on click, compute world coordinates from click position, update `radarStore.setViewport` to center on that position

**Create `src/views/Radar/AlertDetail.tsx`** per UI-SPEC:
- Bottom section of manifest panel
- Header: "AGENT_DETAILS" (Space Grotesk 14px bold uppercase)
- Shows selected agent's recent activity log (last 10 pipeline events for that agent), current file path, intent text
- Scrollable, max height ~300px
- Only visible when an agent is selected

**Update `src/views/RadarView.tsx`:**
- Add RadarManifest panel beside RadarCanvas
- Layout: `<RadarCanvas className="flex-1" />` + `<RadarManifest />`
- RadarManifest conditionally renders based on `isManifestOpen`
- Add AgentTooltip overlay, positioned absolutely within the radar container
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/aitc && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/views/Radar/RadarCanvas.tsx contains "createLinearGradient" for lead line gradient
    - src/views/Radar/RadarCanvas.tsx contains "30000" or "30_000" or "30s" for lead line fade timing
    - src/views/Radar/RadarManifest.tsx contains "AGENT_MANIFEST"
    - src/views/Radar/RadarManifest.tsx contains "280px" or "w-[280px]"
    - src/views/Radar/RadarManifest.tsx imports from "motion" for collapse animation
    - src/views/Radar/AgentManifestRow.tsx contains "getAgentColor"
    - src/views/Radar/AgentManifestRow.tsx contains "selectAgent"
    - src/views/Radar/AgentTooltip.tsx contains "backdrop-blur" or "backdropFilter"
    - src/views/Radar/RadarMinimap.tsx contains "160" and "120" for dimensions
    - src/views/Radar/AlertDetail.tsx contains "AGENT_DETAILS"
    - src/views/RadarView.tsx contains "RadarManifest" and "AgentTooltip"
    - All tests pass
  </acceptance_criteria>
  <done>Radar shows agent trajectory lead lines with gradient opacity and fade animation, collapsible agent manifest panel with click-to-center, glassmorphism agent tooltip on hover, minimap with viewport indicator, and agent details panel.</done>
</task>

<task type="auto">
  <name>Task 2: Component tests for RadarManifest, AgentManifestRow, RadarMinimap</name>
  <files>
    src/views/Radar/__tests__/RadarComponents.test.tsx
  </files>
  <read_first>
    src/views/Radar/RadarManifest.tsx,
    src/views/Radar/AgentManifestRow.tsx,
    src/views/Radar/RadarMinimap.tsx,
    src/stores/__tests__/radarStore.test.ts,
    src/stores/__tests__/agentStore.test.ts
  </read_first>
  <behavior>
    - Test: RadarManifest renders "AGENT_MANIFEST" header when isManifestOpen=true
    - Test: RadarManifest collapse toggle calls radarStore.toggleManifest
    - Test: RadarManifest renders AgentManifestRow for each agent in agentStore
    - Test: AgentManifestRow renders agent ID and colored circle swatch
    - Test: AgentManifestRow click calls radarStore.selectAgent with agent.id
    - Test: AgentManifestRow shows selected state styling when agent is selected
    - Test: RadarMinimap renders at 160x120 dimensions
    - Test: RadarMinimap shows viewport indicator rectangle
  </behavior>
  <action>
Create `src/views/Radar/__tests__/RadarComponents.test.tsx` with vitest + React Testing Library tests.

Mock Zustand stores (radarStore, agentStore, pipelineStore) following existing test patterns. Use `vi.mock` for store modules.

**RadarManifest tests (3 tests):**
- Mock radarStore with `isManifestOpen: true`, render RadarManifest, assert "AGENT_MANIFEST" text visible
- Render RadarManifest, simulate click on collapse toggle button, assert `toggleManifest` was called
- Mock agentStore with 2 agents, render RadarManifest, assert 2 AgentManifestRow items rendered (by agent ID text)

**AgentManifestRow tests (3 tests):**
- Render AgentManifestRow with agent `{ id: "claude-1", agentType: "claude", state: "running" }`, assert "claude-1" text visible and colored circle element present
- Simulate click on row, assert `selectAgent` was called with "claude-1"
- Mock radarStore with `selectedAgentId: "claude-1"`, render row for "claude-1", assert selected styling class applied (e.g., surface-container-high background)

**RadarMinimap tests (2 tests):**
- Render RadarMinimap, assert container has width 160 and height 120 (check style or class)
- Mock radarStore with viewport `{ zoom: 2, panX: 100, panY: 50 }`, render RadarMinimap, assert viewport indicator element is present in DOM
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/aitc && npx vitest run src/views/Radar/__tests__/RadarComponents.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/views/Radar/__tests__/RadarComponents.test.tsx contains at least 8 test cases
    - Tests cover RadarManifest header/collapse/agent-list, AgentManifestRow click/select/render, RadarMinimap dimensions/viewport
    - All tests pass
  </acceptance_criteria>
  <done>Component tests verify RadarManifest collapse behavior and agent listing, AgentManifestRow click-to-select and visual state, RadarMinimap viewport indicator. All tests passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Phase 4 UI</name>
  <action>Human visual verification of both Communications Hub and Airspace Radar views</action>
  <what-built>
Complete Phase 4 UI verification: Communications Hub (3-panel layout with approval queue, inline diff, approve/deny/ask-more/approve-with-edits, chat thread, telemetry panel, mini chat cards) and Airspace Radar (treemap codebase map on Canvas 2D, zoom/pan, agent dots with pulse, lead lines, manifest panel, tooltip, minimap).
  </what-built>
  <how-to-verify>
1. Launch the app: `npm run tauri dev`
2. **Communications Hub (navigate via sidebar to COMMS):**
   - Verify 3-panel layout: left queue (280px), center detail, right telemetry (260px)
   - If no requests: verify "NO_PENDING_REQUESTS" empty state with blinking cursor
   - Verify telemetry panel shows "SYSTEM_LOAD", "TELEMETRY_FEED" sections
   - Verify PendingCountBadge on COMMS sidebar nav item (if requests exist)
   - Keyboard: Arrow Up/Down to navigate queue, A to approve, D to deny

3. **Airspace Radar (navigate via sidebar to RADAR):**
   - If no watch active: verify "AWAITING_SIGNAL" empty state
   - Start a file watcher from Tower Control (click Watch/Deploy)
   - Navigate back to RADAR: verify treemap appears with directory rectangles
   - Mouse wheel: zoom in (should see file labels at 3x, details at 8x)
   - Click-drag: pan the viewport
   - Verify agent dots appear on files being touched (if agents running)
   - Verify right-side manifest panel with "AGENT_MANIFEST" header
   - Click the collapse toggle: panel slides to 0px, canvas expands
   - Verify minimap in bottom-right corner (160x120px)
   - Hover an agent dot: verify glassmorphism tooltip with agent info

4. **Design system compliance:**
   - Dark room aesthetic throughout
   - Phosphor green accents (#8eff71) for approve buttons, healthy states
   - JetBrains Mono for all data/code/diffs
   - Space Grotesk UPPERCASE for section headers
   - Zero-radius corners on all elements
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent intent text -> tooltip | Agent-provided intent displayed in tooltip |
| Pipeline events -> lead lines | File paths from pipeline events rendered on canvas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | AgentTooltip.tsx (intent XSS) | mitigate | Intent text rendered as JSX text node (React escaping). Never use dangerouslySetInnerHTML |
| T-04-13 | Denial of Service | Lead lines (many events) | mitigate | Limit lead lines to last 10 events per agent. Lines older than 30s fade. Sub-pixel culling applies |
</threat_model>

<verification>
- `npx vitest run` all tests pass (including new RadarComponents.test.tsx)
- Radar shows lead lines from agent dots to recently-touched files
- Lead lines fade based on event age (30s fade to 30% opacity)
- Manifest panel collapses/expands with animation
- Agent tooltip shows on hover with glassmorphism
- Minimap shows full treemap with viewport rectangle
- Clicking agent in manifest centers radar on agent
- Human visual verification confirms design system compliance
</verification>

<success_criteria>
User can see agent trajectory lead lines on the radar, hover agents for tooltip details, click agents in the manifest to center the radar, use the minimap for quick navigation, and the full Phase 4 UI passes human visual verification for Command Horizon design system compliance. Component tests verify RadarManifest collapse, AgentManifestRow click-to-select, and RadarMinimap viewport indicator.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-ui-views/04-05-SUMMARY.md`
</output>
