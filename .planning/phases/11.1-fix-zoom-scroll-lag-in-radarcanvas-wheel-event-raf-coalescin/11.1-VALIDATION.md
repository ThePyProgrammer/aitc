---
phase: 11.1
slug: fix-zoom-scroll-lag-in-radarcanvas-wheel-event-raf-coalescin
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-21
---

# Phase 11.1 — Validation Strategy

> Performance-only refactor. Validation is unit-test-driven for regression guards + manual smoke for user-experience acceptance. See 11.1-RESEARCH.md §Validation Architecture for per-decision witness mapping.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.x + jsdom 26 (already installed) |
| **Config file** | `vite.config.ts` (inherited) |
| **Quick run command** | `npm test -- --run src/hooks/__tests__/useCanvasZoomPan.test.ts src/views/Radar/__tests__/hullCache.test.ts` |
| **Full suite command** | `npm test -- --run` |
| **Estimated runtime** | Quick ~5s · Full ~45s |

---

## Sampling Rate

- **After every task commit:** Run quick command (coalescer + cache tests).
- **After every plan wave:** Run full `npm test -- --run` plus `npm run build` (catches TS regressions).
- **Before `/gsd-verify-work`:** Full suite green + manual smoke confirmed.
- **Max feedback latency:** 45 seconds.

---

## Per-Task Verification Map

Single plan (`11.1-01-PLAN.md`); four tasks per RESEARCH §Planner Guidance.

| Task | Locked decision | Test Type | Automated Witness | Status |
|------|-----------------|-----------|-------------------|--------|
| T1: wheel coalescer in useCanvasZoomPan (+ optional useRafCoalesced hook) | D-01, D-02, D-03, D-04 | unit | `grep -Fq "requestAnimationFrame" src/hooks/useCanvasZoomPan.ts` AND `grep -Fq "cancelAnimationFrame" src/hooks/useCanvasZoomPan.ts` AND wheel-coalescer test asserts `setViewport` called exactly once after 10 synthetic wheel events + rAF flush | ⬜ pending |
| T1 test stub | D-14 | unit | `npm test -- --run src/hooks/__tests__/useCanvasZoomPan.test.ts` exits 0 with ≥3 cases (coalesce-multiple, deltaY-accumulation, unmount-cancels) | ⬜ pending |
| T2: defensive rAF throttle on storeSetViewport writeback | D-06, D-07 | unit | `grep -Fq "requestAnimationFrame" src/views/Radar/RadarCanvas.tsx` (within the storeSetViewport useEffect) AND `! grep -qE "useDeferredValue\|useTransition" src/views/Radar/RadarCanvas.tsx` | ⬜ pending |
| T3: hullCache module with settledAt key | D-08, D-09, D-10, D-11 | unit | `test -f src/views/Radar/hullCache.ts` AND hullCache test spies on `d3-polygon.polygonHull` — asserts called once per directory on first draw, zero times on repeat draw with same settledAt, and invalidates on settledAt change | ⬜ pending |
| T3 drawFolderHulls consumes cache | D-08 | unit | `grep -Fq "hullCache" src/views/Radar/GraphRenderer.ts` AND existing `RadarCanvas.test.tsx` still passes | ⬜ pending |
| T3 optional: frustum cull | Claude's Discretion | unit | if added, assert directories with bbox outside viewport are skipped from draw list | ⬜ pending |
| T4: rolling-p95 diagnostic (localStorage-gated) | D-12, D-13 | unit+manual | `grep -Fq "radarPerfDebug" src/views/Radar/RadarCanvas.tsx` AND manual: set `localStorage.radarPerfDebug = '1'` in DevTools, reload, confirm console logs `[RadarPerf] p95=...ms max=...ms` once per ~N frames | ⬜ pending |
| Visual invariance smoke | Phase boundary | manual (UAT) | User launches Tauri prod build, confirms Radar renders pixel-equivalent + zoom-scroll feels responsive on a 5k-node graph | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*This phase has no Wave 0.* All edits land in Wave 1 (single plan, four tasks).

Scaffolding to be created within T1/T3:
- [ ] `src/hooks/__tests__/useCanvasZoomPan.test.ts` — new test file (T1)
- [ ] `src/views/Radar/__tests__/hullCache.test.ts` — new test file (T3)
- [ ] `src/views/Radar/hullCache.ts` — new production module (T3)
- [ ] Optional: `src/hooks/useRafCoalesced.ts` — extracted reusable hook (T1)

*Vitest + jsdom are already installed — no framework install needed.*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Wheel-zoom responsiveness on 5k-node settled graph | D-01 + phase success criterion | Perceived-responsiveness is qualitative; no automated test captures "feels smooth" | Launch `npm run tauri build -- --debug`; open the app; load a ~5k-file repo (AITC itself qualifies); wait for the radar to settle; scroll-wheel to zoom in and out for 3 continuous seconds. **No perceptible lag** is the pass criterion. |
| Diagnostic overlay flows to console | D-12 | Only exercisable in a browser with a DevTools console visible (test env can't observe the actual console) | In the dev server or Tauri DevTools, run `localStorage.setItem('radarPerfDebug', '1')` in the console, reload the app, open the Radar. Expect rolling `[RadarPerf] p95=... max=...` log lines. Clear with `localStorage.removeItem('radarPerfDebug')` and reload to confirm logging stops. |
| Visual invariance vs Phase 11 baseline | Phase boundary | This is a perf refactor; no pixel changes permitted | Launch the prod build; confirm folder hulls, labels, nodes, edges, arrow heads, heat-map, conflict pulses, agent dots all render identically to the Phase 11 closure screenshot/feel. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a manual-only row above
- [ ] Sampling continuity: every task has automated verify (3 manual rows above are UAT, not sampling)
- [ ] Wave 0: N/A (no scaffolding beyond test files created within tasks)
- [ ] No watch-mode flags
- [ ] Feedback latency < 45s (full vitest suite)
- [ ] `nyquist_compliant: true` set once planner has filled per-task ids

**Approval:** pending
