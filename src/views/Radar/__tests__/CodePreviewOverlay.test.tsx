import { describe, it } from 'vitest';

describe('CodePreviewOverlay — Phase 13 Wave 0', () => {
  it.todo('caps rendered signature cards at 6 to satisfy T-13-04 and UI-SPEC line 189');

  it.todo(
    'renders PATH_METADATA and SIGNATURES_UNAVAILABLE — No signature data for this file yet. Showing path metadata instead.',
  );

  it.todo('renders No exported symbols detected. when no exported symbols are available');

  it.todo('expands snippets with EXPAND_SNIPPET and collapses snippets with COLLAPSE_SNIPPET using local state only');

  it.todo('keeps 320px-wide cards within an 8px inset and max height 240px');
});
